(function( $ ) {

    $.fn.darkbox = function(options) {
        
        var settings = $.extend( {
            'bookname' : null,
            
        }, options);
        
            
    };

})(jQuery);
